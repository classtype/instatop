//--------------------------------------------------------------------------------------------------

// Загружаем конфиг
    $.cfg = require('./config');
    
// Соединение базой данных
    $.InitDB(function() {
    // Создаем крон
        $.Cron();
        
    // Создаем крон
        setInterval($.Cron, 60000);
    });
    
// Создаем http сервер
    $.InitHttpServer();
    
//--------------------------------------------------------------------------------------------------